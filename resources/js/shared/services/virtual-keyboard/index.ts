import { init } from "./init";
import { overlaysContent } from "./overlays-content";
import { retainFocus } from "./retain-focus";

export const VirtualKeyboard = {
    init,
    overlaysContent,
    retainFocus
};
