---
paths:
  - 'resources/js/**'
---

# Js

## Use Inertia Svelte for application pages
Build application pages and client UI with Inertia Svelte; keep Blade limited to the Inertia shell and framework mail views.

## Organize frontend code by application layer
Organize Svelte code into app, pages, widgets, features, entities, and shared layers; expose reusable slices through index.ts entrypoints.
