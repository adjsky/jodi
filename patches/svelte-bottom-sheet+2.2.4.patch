diff --git a/node_modules/svelte-bottom-sheet/dist/BottomSheet/BottomSheet.svelte b/node_modules/svelte-bottom-sheet/dist/BottomSheet/BottomSheet.svelte
index 4777da0..cd09d11 100644
--- a/node_modules/svelte-bottom-sheet/dist/BottomSheet/BottomSheet.svelte
+++ b/node_modules/svelte-bottom-sheet/dist/BottomSheet/BottomSheet.svelte
@@ -18,6 +18,7 @@ to has "height" in it's name.
 		isSheetOpen = $bindable(false),
 		onopen,
 		onclose,
+		onclosecomplete,
 		onsheetdrag,
 		onsheetdragstart,
 		onsheetdragend,
@@ -40,6 +41,7 @@ to has "height" in it's name.
 		children: Snippet<[]>;
 		onopen?: () => void;
 		onclose?: () => void;
+		onclosecomplete?: () => void;
 		onsheetdrag?: () => void;
 		onsheetdragstart?: () => void;
 		onsheetdragend?: () => void;
@@ -370,6 +372,20 @@ to has "height" in it's name.
 		if (settings.disableDragging) return;
 		onsheetdragend?.();
 
+        // Determine movement direction (snapping up or down)
+		const isMovingUp = sheetHeight < startHeight;
+		const buffer = measurementToPx(0.95, maxHeightPx);
+
+		// Prevent accidental snapping if the movement is small
+		if (
+			(isMovingUp && sheetHeight > startHeight - buffer) ||
+			(!isMovingUp && sheetHeight < startHeight + buffer)
+		) {
+			sheetHeight = startHeight;
+			resetStatesAfterMove();
+			return;
+		}
+
 		// If there is only one snap point (1), apply a larger buffer for closing behavior
 		if (settings.snapPoints.length === 1) {
 			if (sheetHeight > measurementToPx(settings.closeThreshold, maxHeightPx)) {
@@ -393,20 +409,6 @@ to has "height" in it's name.
 			return;
 		}
 
-		// Determine movement direction (snapping up or down)
-		const isMovingUp = sheetHeight < startHeight;
-		const buffer = measurementToPx(0.95, maxHeightPx);
-
-		// Prevent accidental snapping if the movement is small
-		if (
-			(isMovingUp && sheetHeight > startHeight - buffer) ||
-			(!isMovingUp && sheetHeight < startHeight + buffer)
-		) {
-			sheetHeight = startHeight;
-			resetStatesAfterMove();
-			return;
-		}
-
 		// Find valid snap points based on the movement direction
 		const validPoints = settings.snapPoints
 			.map((point) => ({ original: point, converted: measurementToPx(point, maxHeightPx) }))
@@ -502,7 +504,8 @@ to has "height" in it's name.
 		},
 		toggleSheet: () => {
 			isSheetOpen = !isSheetOpen;
-		}
+		},
+		closeComplete: onclosecomplete
 	};
 
 	let sheetIdentifcationContext: SheetIdentificationContext = {
diff --git a/node_modules/svelte-bottom-sheet/dist/BottomSheet/BottomSheet.svelte.d.ts b/node_modules/svelte-bottom-sheet/dist/BottomSheet/BottomSheet.svelte.d.ts
index 35600ce..bb3f5af 100644
--- a/node_modules/svelte-bottom-sheet/dist/BottomSheet/BottomSheet.svelte.d.ts
+++ b/node_modules/svelte-bottom-sheet/dist/BottomSheet/BottomSheet.svelte.d.ts
@@ -6,6 +6,7 @@ type $$ComponentProps = {
     children: Snippet<[]>;
     onopen?: () => void;
     onclose?: () => void;
+    onclosecomplete?: () => void;
     onsheetdrag?: () => void;
     onsheetdragstart?: () => void;
     onsheetdragend?: () => void;
diff --git a/node_modules/svelte-bottom-sheet/dist/BottomSheet/Overlay/Overlay.svelte b/node_modules/svelte-bottom-sheet/dist/BottomSheet/Overlay/Overlay.svelte
index c372f2b..34f5a37 100644
--- a/node_modules/svelte-bottom-sheet/dist/BottomSheet/Overlay/Overlay.svelte
+++ b/node_modules/svelte-bottom-sheet/dist/BottomSheet/Overlay/Overlay.svelte
@@ -14,11 +14,19 @@
 		children,
 		...rest
 	}: { children?: Snippet<[]> | undefined } & HTMLAttributes<HTMLDivElement> = $props();
+
+	function onclick() {
+		if (sheetContext.settings.disableClosing) {
+			return;
+    	}
+		sheetContext.closeSheet();
+	}
 </script>
 
 {#if sheetContext.isSheetOpen}
 	<div
 		{...rest}
+		{onclick}
 		role="presentation"
 		aria-hidden="true"
 		transition:fade={{ duration: 200 }}
diff --git a/node_modules/svelte-bottom-sheet/dist/BottomSheet/Sheet/Sheet.svelte b/node_modules/svelte-bottom-sheet/dist/BottomSheet/Sheet/Sheet.svelte
index c5bdf9e..f8f8ecc 100644
--- a/node_modules/svelte-bottom-sheet/dist/BottomSheet/Sheet/Sheet.svelte
+++ b/node_modules/svelte-bottom-sheet/dist/BottomSheet/Sheet/Sheet.svelte
@@ -15,25 +15,17 @@
 		'sheetIdentificationContext'
 	);
 
+    if (!sheetContext) {
+		throw new Error('BottomSheet.Sheet must be inside a BottomSheet component');
+	}
+
 	let previousActiveElement: HTMLElement | null;
 	let axisForSlide: 'x' | 'y' =
 		sheetContext.settings.position === 'left' || sheetContext.settings.position === 'right'
 			? 'x'
 			: 'y';
 
-	if (!sheetContext) {
-		throw new Error('BottomSheet.Sheet must be inside a BottomSheet component');
-	}
-
-	const handleClickOutside = (event: MouseEvent) => {
-		if (
-			sheetContext.sheetElement &&
-			!sheetContext.sheetElement.contains(event.target as Node) &&
-			!sheetContext.settings.disableClosing
-		) {
-			sheetContext.closeSheet();
-		}
-	};
+	const isSheetOpen = $derived(sheetContext.isSheetOpen);
 
 	const getFocusableElements = () => {
 		if (!sheetContext.sheetElement) return [];
@@ -124,7 +116,7 @@
 	};
 
 	$effect(() => {
-		if (sheetContext.isSheetOpen) {
+		if (isSheetOpen) {
 			previousActiveElement = document.activeElement as HTMLElement;
 			setTimeout(() => {
 				const focusableElements = getFocusableElements();
@@ -138,10 +130,8 @@
 				} else {
 					sheetContext.sheetElement?.focus();
 				}
-				document.addEventListener('click', handleClickOutside);
 			}, 100);
 		} else {
-			document.removeEventListener('click', handleClickOutside);
 			previousActiveElement?.focus();
 		}
 	});
@@ -169,6 +159,7 @@
 		onmousedown={sheetContext.mouseDownEvent}
 		onmouseup={sheetContext.moveEnd}
 		onwheel={stopScrollPropagationWheel}
+		onoutroend={sheetContext.closeComplete}
 		transition:slideTransition={{
 			duration: 500,
 			easing: cubicOut,
diff --git a/node_modules/svelte-bottom-sheet/dist/types.d.ts b/node_modules/svelte-bottom-sheet/dist/types.d.ts
index c0c9334..1efd46b 100644
--- a/node_modules/svelte-bottom-sheet/dist/types.d.ts
+++ b/node_modules/svelte-bottom-sheet/dist/types.d.ts
@@ -65,4 +65,5 @@ export type SheetContext = {
     openSheet: () => void;
     closeSheet: () => void;
     toggleSheet: () => void;
+    closeComplete: () => void;
 };
diff --git a/node_modules/svelte-bottom-sheet/dist/utils.js b/node_modules/svelte-bottom-sheet/dist/utils.js
index 6dab404..c9ec27f 100644
--- a/node_modules/svelte-bottom-sheet/dist/utils.js
+++ b/node_modules/svelte-bottom-sheet/dist/utils.js
@@ -32,7 +32,7 @@ export const getScrollableElement = (element) => {
         const hasScrollableX = overflowX !== 'visible' &&
             overflowX !== 'hidden' &&
             element.scrollWidth > element.clientWidth;
-        if (element.className.split(' ').includes('bottom-sheet') &&
+        if (element.className.split?.(' ').includes('bottom-sheet') &&
             (hasScrollableY || hasScrollableX)) {
             return element;
         }
