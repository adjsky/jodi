diff --git a/node_modules/@inertiajs/core/dist/index.js b/node_modules/@inertiajs/core/dist/index.js
index 4ebfdc7..5172bdf 100644
--- a/node_modules/@inertiajs/core/dist/index.js
+++ b/node_modules/@inertiajs/core/dist/index.js
@@ -1095,9 +1095,7 @@ var CurrentPage = class {
       }
       page2.rememberedState ??= {};
       const isServer3 = typeof window === "undefined";
-      const location = !isServer3 ? window.location : new URL(page2.url);
       const scrollRegions = !isServer3 && preserveScroll ? Scroll.getScrollRegions() : [];
-      replace = replace || isSameUrlWithoutHash(hrefToUrl(page2.url), location);
       const pageForHistory = { ...page2, flash: {} };
       return new Promise(
         (resolve) => replace ? history.replaceState(pageForHistory, resolve) : history.pushState(pageForHistory, resolve)
@@ -1146,13 +1144,14 @@ var CurrentPage = class {
     });
   }
   setQuietly(page2, {
-    preserveState = false
+    preserveState = false,
+    viewTransition = false
   } = {}) {
     return this.resolve(page2.component, page2).then((component) => {
       this.page = page2;
       this.cleared = false;
       history.setCurrent(page2);
-      return this.swap({ component, page: page2, preserveState, viewTransition: false });
+      return this.swap({ component, page: page2, preserveState, viewTransition });
     });
   }
   clear() {
@@ -1202,9 +1201,11 @@ var CurrentPage = class {
     if (!viewTransition || !document?.startViewTransition || document.visibilityState === "hidden") {
       return doSwap();
     }
-    const viewTransitionCallback = typeof viewTransition === "boolean" ? () => null : viewTransition;
+    const viewTransitionCallback = typeof viewTransition === "function" ? viewTransition : () => null;
     return new Promise((resolve) => {
-      const transitionResult = document.startViewTransition(() => doSwap().then(resolve));
+      const update = () => doSwap().then(resolve);
+      const supportsTypes = typeof ViewTransition !== "undefined" && "types" in ViewTransition.prototype;
+      const transitionResult = typeof viewTransition === "object" && supportsTypes ? document.startViewTransition({ update, types: viewTransition.types }) : document.startViewTransition(update);
       transitionResult.ready.catch(() => {
       });
       viewTransitionCallback(transitionResult);
@@ -1575,6 +1576,9 @@ var EventHandler = class {
     page.clear();
     this.fireInternalEvent("missingHistoryItem");
   }
+  onPopstateFinish() {
+    this.fireInternalEvent("popstateFinish");
+  }
   fireInternalEvent(event, ...args) {
     this.internalListeners.filter((listener) => listener.event === event).forEach((listener) => listener.listener(...args));
   }
@@ -1597,14 +1601,27 @@ var EventHandler = class {
       url.hash = window.location.hash;
       history.replaceState({ ...page.getWithoutFlashData(), url: url.href });
       Scroll.reset();
+      this.onPopstateFinish();
       return;
     }
     if (!history.isValidState(state)) {
-      return this.onMissingHistoryItem();
+      this.onMissingHistoryItem();
+      this.onPopstateFinish();
+      return;
     }
     history.decrypt(state.page).then((data) => {
       if (page.get().version !== data.version) {
         this.onMissingHistoryItem();
+        this.onPopstateFinish();
+        return;
+      }
+      if (page.get().__jodi_historyModals?.length) {
+        const { viewTransition } = page.get().__jodi_historyModals.pop();
+        const page2 = { ...page.get(), url: data.url };
+        page.setQuietly(page2, {
+          preserveState: true,
+          viewTransition: viewTransition ? { types: ["back"] } : false
+        }).then(() => this.onPopstateFinish());
         return;
       }
       router.cancelAll({ prefetch: false });
@@ -1622,9 +1639,11 @@ var EventHandler = class {
         if (Object.keys(pendingDeferred).length > 0) {
           this.fireInternalEvent("loadDeferredProps", pendingDeferred);
         }
+        this.onPopstateFinish();
       });
     }).catch(() => {
       this.onMissingHistoryItem();
+      this.onPopstateFinish();
     });
   }
 };
@@ -1735,7 +1754,11 @@ var InitialVisit = class {
       page.setUrlHash(window.location.hash);
     }
     const visitId = uid();
-    page.set(page.get(), { preserveScroll: true, preserveState: true, initialRender: true, visitId }).then(() => {
+    const page2 = {
+      ...page.get(),
+      __jodi_historyModals: history.initialState?.__jodi_historyModals ?? []
+    };
+    page.set(page2, { preserveScroll: true, preserveState: true, replace: true, initialRender: true, visitId }).then(() => {
       if (navigationType.isReload()) {
         Scroll.restore(history.getScrollRegions());
       } else {
@@ -2611,6 +2634,9 @@ var Response = class _Response {
     await this.setRememberedState(pageResponse);
     this.requestParams.setPreserveOptions(pageResponse);
     pageResponse.url = history.preserveUrl ? page.get().url : this.pageUrl(pageResponse);
+    if (history.preserveUrl) {
+      pageResponse.__jodi_historyModals = page.get().__jodi_historyModals;
+    }
     this.requestParams.all().onBeforeUpdate(pageResponse);
     fireBeforeUpdateEvent(pageResponse);
     return page.set(pageResponse, {
@@ -3325,8 +3351,22 @@ var Router = class {
   resolveComponent(component, page2) {
     return page.resolve(component, page2);
   }
+  back() {
+    return new Promise((resolve, reject) => {
+      const unsubscribe = eventHandler.on("popstateFinish", () => {
+        unsubscribe();
+        clearTimeout(timeout);
+        resolve();
+      });
+      const timeout = setTimeout(() => {
+        unsubscribe();
+        reject(new Error("Timed out waiting for history navigation."));
+      }, 1e3);
+      window.history.back();
+    });
+  }
   replace(params) {
-    this.clientVisit(params, { replace: true });
+    return this.clientVisit(params, { replace: true });
   }
   replaceProp(name, value, options) {
     this.replace({
@@ -3366,7 +3406,7 @@ var Router = class {
     );
   }
   push(params) {
-    this.clientVisit(params);
+    return this.clientVisit(params);
   }
   flash(keyOrData, value) {
     const current = page.get().flash;
@@ -3386,7 +3426,7 @@ var Router = class {
     }
   }
   clientVisit(params, { replace = false } = {}) {
-    this.clientVisitQueue.add(() => this.performClientVisit(params, { replace }));
+    return this.clientVisitQueue.add(() => this.performClientVisit(params, { replace }));
   }
   performClientVisit(params, { replace = false } = {}) {
     const current = page.get();
@@ -3398,13 +3438,16 @@ var Router = class {
     ) : {};
     const props = typeof params.props === "function" ? params.props(current.props, onceProps) : params.props ?? current.props;
     const flash = typeof params.flash === "function" ? params.flash(current.flash) : params.flash;
-    const { viewTransition, onError, onFinish, onFlash, onSuccess, ...pageParams } = params;
+    const { viewTransition, onError, onFinish, onFlash, onSuccess, __jodi_isHistoryModal, ...pageParams } = params;
     const page2 = {
       ...current,
       ...pageParams,
       flash: flash ?? {},
       props
     };
+    if (__jodi_isHistoryModal && !replace) {
+      (page2.__jodi_historyModals ??= []).push({ viewTransition: Boolean(viewTransition) });
+    }
     const preserveScroll = RequestParams.resolvePreserveOption(params.preserveScroll ?? false, page2);
     const preserveState = RequestParams.resolvePreserveOption(params.preserveState ?? false, page2);
     const visitId = this.createVisitId();
@@ -3561,6 +3604,7 @@ var Router = class {
       onStart: options.onStart || noop,
       onProgress: options.onProgress || noop,
       onFinish: options.onFinish || noop,
+      onOptimisticRollback: options.onOptimisticRollback || noop,
       onCancel: options.onCancel || noop,
       onSuccess: options.onSuccess || noop,
       onError: options.onError || noop,
@@ -3607,6 +3651,7 @@ var Router = class {
         if (Object.keys(replayedProps).length > 0) {
           page.setPropsQuietly({ ...page.get().props, ...replayedProps });
         }
+        events.onOptimisticRollback(visit);
       }
       if (page.pendingOptimisticCount() === 0) {
         page.clearOptimisticState();
diff --git a/node_modules/@inertiajs/core/types/page.d.ts b/node_modules/@inertiajs/core/types/page.d.ts
index dd61664..9897181 100644
--- a/node_modules/@inertiajs/core/types/page.d.ts
+++ b/node_modules/@inertiajs/core/types/page.d.ts
@@ -29,8 +29,11 @@ declare class CurrentPage {
         initialRender?: boolean;
         visitId?: string;
     }): Promise<void>;
-    setQuietly(page: Page, { preserveState, }?: {
+    setQuietly(page: Page, { preserveState, viewTransition, }?: {
         preserveState?: boolean;
+        viewTransition?: Visit['viewTransition'] | {
+            types: string[];
+        };
     }): Promise<unknown>;
     clear(): void;
     isCleared(): boolean;
diff --git a/node_modules/@inertiajs/core/types/router.d.ts b/node_modules/@inertiajs/core/types/router.d.ts
index 71a54c5..842885f 100644
--- a/node_modules/@inertiajs/core/types/router.d.ts
+++ b/node_modules/@inertiajs/core/types/router.d.ts
@@ -44,15 +44,16 @@ export declare class Router {
     clearHistory(): void;
     decryptHistory(): Promise<Page>;
     resolveComponent(component: string, page?: Page): Promise<Component>;
-    replace<TProps = Page['props']>(params: ClientSideVisitOptions<TProps>): void;
+    back(): Promise<void>;
+    replace<TProps = Page['props']>(params: ClientSideVisitOptions<TProps>): Promise<void>;
     replaceProp<TProps = Page['props']>(name: string, value: unknown | ((oldValue: unknown, props: TProps) => unknown), options?: Pick<ClientSideVisitOptions, 'onError' | 'onFinish' | 'onSuccess'>): void;
     appendToProp<TProps = Page['props']>(name: string, value: unknown | unknown[] | ((oldValue: unknown, props: TProps) => unknown | unknown[]), options?: Pick<ClientSideVisitOptions, 'onError' | 'onFinish' | 'onSuccess'>): void;
     prependToProp<TProps = Page['props']>(name: string, value: unknown | unknown[] | ((oldValue: unknown, props: TProps) => unknown | unknown[]), options?: Pick<ClientSideVisitOptions, 'onError' | 'onFinish' | 'onSuccess'>): void;
-    push<TProps = Page['props']>(params: ClientSideVisitOptions<TProps>): void;
+    push<TProps = Page['props']>(params: ClientSideVisitOptions<TProps>): Promise<void>;
     flash<TFlash extends PageFlashData = PageFlashData>(keyOrData: string | ((flash: FlashData) => TFlash) | TFlash, value?: unknown): void;
     protected clientVisit<TProps = Page['props']>(params: ClientSideVisitOptions<TProps>, { replace }?: {
         replace?: boolean;
-    }): void;
+    }): Promise<void>;
     protected performClientVisit<TProps = Page['props']>(params: ClientSideVisitOptions<TProps>, { replace }?: {
         replace?: boolean;
     }): Promise<void>;
diff --git a/node_modules/@inertiajs/core/types/types.d.ts b/node_modules/@inertiajs/core/types/types.d.ts
index bb00e9e..a294150 100644
--- a/node_modules/@inertiajs/core/types/types.d.ts
+++ b/node_modules/@inertiajs/core/types/types.d.ts
@@ -156,6 +156,9 @@ export interface Page<SharedProps extends PageProps = PageProps> {
         prop: keyof PageProps;
         expiresAt?: number | null;
     }>;
+    __jodi_historyModals?: Array<{
+        viewTransition?: boolean;
+    }>;
     /** @internal */
     rememberedState: Record<string, unknown>;
     /** @internal */
@@ -176,6 +179,7 @@ export interface ClientSideVisitOptions<TProps = Page['props']> {
     preserveState?: VisitOptions['preserveState'];
     errorBag?: string | null;
     viewTransition?: VisitOptions['viewTransition'];
+    __jodi_isHistoryModal?: boolean;
     onError?: (errors: Errors) => void;
     onFinish?: (visit: ClientSideVisitOptions<TProps>) => void;
     onFlash?: (flash: FlashData) => void;
@@ -366,7 +370,7 @@ export type GlobalEventResult<TEventName extends GlobalEventNames<T>, T extends
 export type GlobalEventDetails<TEventName extends GlobalEventNames<T>, T extends RequestPayload = RequestPayload> = GlobalEventsMap<T>[TEventName]['details'];
 export type GlobalEventTrigger<TEventName extends GlobalEventNames<T>, T extends RequestPayload = RequestPayload> = (...params: GlobalEventParameters<TEventName, T>) => GlobalEventResult<TEventName, T>;
 export type GlobalEventCallback<TEventName extends GlobalEventNames<T>, T extends RequestPayload = RequestPayload> = (...params: GlobalEventParameters<TEventName, T>) => GlobalEventResult<TEventName, T>;
-export type InternalEvent = 'missingHistoryItem' | 'loadDeferredProps' | 'historyQuotaExceeded';
+export type InternalEvent = 'missingHistoryItem' | 'loadDeferredProps' | 'historyQuotaExceeded' | 'popstateFinish';
 export type VisitCallbacks<T extends RequestPayload = RequestPayload> = {
     onCancelToken: CancelTokenCallback;
     onBefore: GlobalEventCallback<'before', T>;
@@ -374,6 +378,7 @@ export type VisitCallbacks<T extends RequestPayload = RequestPayload> = {
     onStart: GlobalEventCallback<'start', T>;
     onProgress: GlobalEventCallback<'progress', T>;
     onFinish: GlobalEventCallback<'finish', T>;
+    onOptimisticRollback: (visit: ActiveVisit<T>) => void;
     onCancel: GlobalEventCallback<'cancel', T>;
     onSuccess: GlobalEventCallback<'success', T>;
     onError: GlobalEventCallback<'error', T>;
