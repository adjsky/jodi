import { init } from "./init.ts";
import { overlaysContent } from "./overlays-content.ts";
import { retainFocus } from "./retain-focus.ts";

export const VirtualKeyboard = {
    init,
    overlaysContent,
    retainFocus
};
