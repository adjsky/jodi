pre-commit:
  jobs:
    - run: "composer run format"
      glob: "*.php"
      stage_fixed: true
    - run: "npm run lint {staged_files} -- --fix"
      glob: "*.{js,ts,svelte}"
      stage_fixed: true
pre-push:
  jobs:
    - run: "composer run analyse"
      glob: "*.php"
